import { Batch } from "@/types/batch";
import { Order } from "@/types/order";

export function RecentActivityList({
  orders,
  batches,
}: {
  orders: Order[];
  batches: Batch[];
}) {
  return <>{/* activity JSX */}</>;
}
